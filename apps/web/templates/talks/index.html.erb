<h2>All talks</h2>

<div id="talks">
  <% if talks.any? %>
    <% talks.each do |talk| %>
      <div class="talk">
        <h2><%= talk.title %></h2>
        <p><%= talk.author %></p>
      </div>
    <% end %>
  <% else %>
    <p class="placeholder">No talks yet :(</p>
  <% end %>
</div>
